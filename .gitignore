# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Data / models không commit (dung lượng lớn hoặc vướng license)
# LƯU Ý: .gitignore KHÔNG hỗ trợ comment cùng dòng — comment phải ở dòng riêng.
# i2b2/n2c2 vướng DUA — KHÔNG redistribute
data/external/**
# file gốc ICD-10 BYT / RxNorm RRF (lớn) — chỉ ship script tải + README hướng dẫn
data/kb/raw/**
!data/kb/raw/README.md
# KB parquet (ICD-10 BYT + RxNorm SCD) nhỏ & được phép phân phối -> COMMIT để dựng lại
# không cần tải/đăng ký; brand map auto sinh từ RRF cũng ship kèm.
# Embedding index semantic (T2) — lớn, rebuild bằng src/kb/build_icd_index.py trên GPU
data/kb/*_emb.npy
data/kb/*_meta.parquet
data/kb/icd10cm_index_meta.parquet
models/*.safetensors
models/*.bin
models/*.gguf
*.faiss

# Output
output/
*.zip

# Dev review HTML — tự sinh lại bằng src/datagen/build_review_html.py
data/dev/review/

# Notebook checkpoints
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db
