# Core - du cho: rule pipeline, lexical linking, eval, build KB.
# (KHONG chua transformers/FlagEmbedding de tranh xung dot tren Colab/Kaggle;
#  chay LLM -> requirements-gpu.txt; linking semantic v1 -> requirements-semantic.txt)
#
# Dung RANGE (>=), KHONG pin cung: pin numpy==1.26 ha cap numpy 2.x cua Colab
# -> vo ABI ("numpy.dtype size changed") voi torch/transformers build cho numpy 2.
pyyaml>=6.0
tqdm>=4.66
numpy>=1.26                  # de nguyen numpy 2.x co san cua Colab/Kaggle (KHONG ha cap)
pandas>=2.2
pyarrow>=17                  # doc/ghi parquet (KB ICD-10 / RxNorm)
xlrd>=2.0                    # doc Danh muc ICD-10 BYT dinh dang .xls (BIFF cu)
openpyxl>=3.1               # doc .xlsx (neu BYT phat hanh ban moi)
rapidfuzz>=3.10
