# BẢN NỘP CHÍNH THỨC (--backend rule) — deps ĐÃ PIN, KHÔNG torch/GPU/internet.
# BTC dựng lại chỉ cần: pip install -r requirements-submit.txt
# (LLM/RAG là thí nghiệm, KHÔNG dùng khi nộp — deps của chúng ở requirements-lock.txt / -semantic.txt.)
numpy==2.0.2
pandas==2.2.3
pyarrow==17.0.0
rapidfuzz==3.10.1
pyyaml==6.0.2
tqdm==4.67.1
